spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/userdb
    username: user1
    password: '0000'
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8084